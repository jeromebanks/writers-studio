[project]
name = "writers-studio-worker"
version = "0.0.0"
description = "Python worker for embeddings, stylometry, UMAP, and narrative analysis (Stage 6+)."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/writers_studio_worker"]
