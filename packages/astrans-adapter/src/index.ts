// Stage 2 (ROADMAP.md): Astrans upstream adapter. Clones/fetches
// https://github.com/jeromebanks/astrans.git into .worktrees/, resolves a
// ref to a commit SHA, and imports a deliberate vertical slice with
// provenance. Never imports novel/, game assets, or build output; never
// pushes back to Astrans.
export {};
